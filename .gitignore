# OS / IDE
.DS_Store
.graymatter/
__pycache__/
*.pyc
plugin.video.flexitv*.zip

# Bundled Python dependencies (rebuilt by build.sh)
plugin.video.flexitv/resources/lib/python-deps/

# Python venv / pip scratch dirs (used during build)
py39-deps/
